# ══════════════════════════════════════════════════════════════════
# Forecasting Platform — requirements.txt
# Version : 2.0.0 (Phase 1)
# Replaces the full system package dump (~250 packages) from v1.0
# which included unrelated CV/vision libraries (ultralytics, roboflow,
# cvzone, pycocotools, deep-sort-realtime, super-gradients, torch,
# torchvision) adding ~3-4GB to Docker images unnecessarily.
# ══════════════════════════════════════════════════════════════════

# ── API Framework ─────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9        # file upload support
httpx==0.27.0                  # async HTTP client + test client

# ── Data Validation ───────────────────────────────────────────────
pydantic==2.10.3
pydantic-settings==2.3.0       # Settings management from env vars
python-dotenv==1.0.1

# ── Auth ──────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT tokens
passlib[bcrypt]==1.7.4             # password hashing

# ── Database ──────────────────────────────────────────────────────
sqlalchemy==2.0.36
asyncpg==0.30.0                # async PostgreSQL driver
alembic==1.14.0                # DB migrations

# ── Async Task Queue ──────────────────────────────────────────────
celery==5.4.0
redis==5.2.0                   # Redis client (Celery broker + cache)

# ── AWS ───────────────────────────────────────────────────────────
boto3==1.35.95

# ── Core Data / ML ────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.7.2
scipy==1.15.1
statsmodels==0.14.1

# ── Forecasting Models ────────────────────────────────────────────
# NOTE: xgboost's default PyPI wheel bundles GPU/CUDA extras
# (nvidia-nccl-cu12, ~300MB) even on CPU-only-typical workloads, causing
# slow/flaky installs. We pin CPU-only here since typical dataset sizes
# (thousands of rows) don't benefit from GPU acceleration anyway.
# To re-enable GPU later: pip install xgboost[gpu] or rebuild lightgbm
# with --config-settings=cmake.define.USE_CUDA=ON — no code changes needed.
lightgbm==4.5.0
xgboost==3.1.3
prophet==1.1.6
tbats==1.1.3
pmdarima==2.0.4                # auto-ARIMA (TD-006 fix)

# ── Visualisation ─────────────────────────────────────────────────
plotly==5.24.1

# ── Utilities ─────────────────────────────────────────────────────
holidays==0.59
openpyxl==3.1.5                # Excel file support
python-dateutil==2.9.0.post0

# ── Logging ───────────────────────────────────────────────────────
structlog==24.4.0              # structured JSON logging

# ── Testing ───────────────────────────────────────────────────────
pytest==8.3.0
pytest-asyncio==0.23.0
pytest-cov==5.0.0

# ── Dev only (not in production Docker image) ─────────────────────
# black==24.0.0
# ruff==0.4.0